//Euler 5: Smallest Remainder
//faster method using prime factors
//we know the prime factors can be multiplied to get the remainder of a number
//we can build an algorithm using the prime factorization of 20

primeFactors20() {
	var k = 20;
	var Primes = [];
	Primes.push(2);

	if (k % (2 * i) == 0 && i < j) {
		Primes.push(i)
	}
	i++
}